<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.loveword">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="ACCESS_NETWORK_STATE" />

    <application
        android:name=".Application.WordApplication"
        android:allowBackup="true"
        android:icon="@drawable/word"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".framwork.Activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".framwork.Activity.StudyActivity" />
        <activity android:name=".framwork.Activity.WrongActivity" />
        <activity android:name=".framwork.Activity.MyWordActivity" />
        <activity android:name=".framwork.Activity.SearchActivity" />
        <activity android:name=".framwork.Activity.MainActivity" />
        <activity android:name=".framwork.Activity.AboutActivity" />
        <activity android:name=".framwork.Activity.MyPathActivity"></activity>
    </application>

</manifest>